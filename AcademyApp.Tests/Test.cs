using System;
using System.Collections.Generic;
using System.Text;
using AcademyApp.API.Infrastructure.Mapping;
using AcademyApp.Data.EF.Implementation;
using AcademyApp.Data.EF.Interface;
using AcademyApp.Domain.POCO;
using AcademyApp.Service.Implementation;
using AutoMapper;
using AutoMapper.Mappers;
using Xunit;

namespace AcademyApp.Tests
{
    public class Test
    {
        [Fact]
        public void FirstTest()
        {

            // var context = MakeNewClasses.GetContext();
            // var eventRepo = MakeNewClasses.GetEventRepo(context);
            // var userRepo = MakeNewClasses.GetUserRepository(context);
            // var imapper = new Mapper(new AutoMapperConfig);
            // var userEvent = new EventService(eventRepo);
        }
    }
}
